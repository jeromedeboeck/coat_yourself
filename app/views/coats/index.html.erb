<div class="container">
  <div class="row">
    <div class="col-xs-12 col-sm-4 col-sm-offset-4">
      <%= render "forms/category_search" %>
    </div>
    <div class="col-xs-12 col-sm-4 col-sm-offset-4">
      <h3> Test Result </h3>
      <% @coats.each do |coat| %>
        <%= coat.name %>
      <% end %>
    </div>
  </div>
</div>
